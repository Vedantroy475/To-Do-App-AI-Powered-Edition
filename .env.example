# Authentication + AI Keys
OPENROUTER_API_KEY="REPLACE_WITH_OPENROUTER_API_KEY"
JWT_SECRET="REPLACE_WITH_JWT_SECRET"
EMBED_API_KEY="REPLACE_WITH_EMBED_API_KEY"

# Embedding Service (FastAPI / Lightning endpoint)
# Use localhost while developing or your deployed endpoint URL in production.
EMBED_SERVICE_URL=http://127.0.0.1:8000
# EMBED_SERVICE_URL=https://your-cloudrunhosted-service.example.com

# CockroachDB connection (SQL storage for users + embeddings)
DATABASE_URL="postgresql://<username>:<password>@<host>:26257/<database_name>?sslmode=verify-full"

